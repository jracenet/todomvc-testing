import { module, test } from 'qunit';
import { visit, currentURL } from '@ember/test-helpers';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | bulk update', function(hooks) {
  setupApplicationTest(hooks);

  test('Can mark multiple items as done', async function (assert) {});
  test('Can mark all items as done', async function (assert) {});
  test('Can mark all items as to do', async function (assert) {});
  test('Can delete all selected items', async function (assrt) {});
});
